#include <tunables/global>

profile tronbyt_server flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Allow basic file ops
  file,

  # Read the application
  /app/** rix,

  # Persisted add-on data
  /data/** rwk,

  # Temp
  /tmp/** rwk,

  # Allow networking
  network,
}
